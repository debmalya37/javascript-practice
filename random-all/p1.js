// check if array or not

const { Console } = require("console");

let word = ["bag", "doll", "school", "heart"];

console.log(Array.isArray(word));

// function params

const person = {
  name: "deb",
  id: 1,
  country: "ind",
};

console.log(typeof person);
